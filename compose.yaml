services:
  abs-ranobedb:
    # build: .
    image: ghcr.io/kennethsible/abs-ranobedb
    container_name: abs-ranobedb
    environment:
      LOG_LEVEL: "INFO"
    ports:
      - "5000:5000"
    restart: unless-stopped
